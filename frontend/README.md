## Frontend (React → Vercel)

This is a Vite + React + TypeScript app with a modern dashboard layout.

### Env vars

- `VITE_API_BASE_URL` (e.g. `https://your-cloud-run-service.a.run.app`)
- `VITE_INSTANTDB_APP_ID` (InstantDB app id)

### Local dev

```bash
cd frontend
npm install
npm run dev
```

